DATABASE_URL=postgresql+psycopg://bhuarjan:bhuarjan_dev@localhost:5433/bhuarjan
MINIO_ENDPOINT=localhost:9100
MINIO_ACCESS_KEY=bhuarjan
MINIO_SECRET_KEY=bhuarjan_dev
MINIO_SECURE=false
MINIO_PUBLIC_ENDPOINT=localhost:9100
JWT_SECRET=dev-secret-change-me
# AES-256-GCM key for affected-family PII, 64 hex characters (Docs/rules.md C5).
# Left commented so the demo falls back to the published development constant.
# PRODUCTION MUST SUPPLY A KMS-BACKED KEY: with DEMO_MODE=false the API refuses to
# start until PII_KEY holds a real 64-hex key.
#   python -c "import os;print(os.urandom(32).hex())"
# PII_KEY=
DEMO_MODE=true
SEED_ON_START=true
# LLM_EXTRACTION_URL=   # empty → regex-only extraction (offline demo)
